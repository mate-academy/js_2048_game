import slideTiles from './slideTiles.js';

function moveUp(grid) {
  return slideTiles(grid);
}

function moveDown(grid) {
  return slideTiles(grid);
}

function moveLeft(grid) {
  return slideTiles(grid);
}

function moveRight(grid) {
  return slideTiles(grid);
}

export {
  moveUp,
  moveDown,
  moveLeft,
  moveRight,
};
