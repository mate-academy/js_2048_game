const STATUS_IDLE = 'idle';
const STATUS_PLAYING = 'playing';
const STATUS_WIN = 'win';
const STATUS_LOSE = 'lose';

module.exports = {
  STATUS_IDLE,
  STATUS_PLAYING,
  STATUS_WIN,
  STATUS_LOSE,
};
